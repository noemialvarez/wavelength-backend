[build]
builder = "nixpacks"
nixpacksVersion = "1"

[build.env]
NODE_VERSION = "20"

[build.cache]
paths = ["node_modules"]

[deploy]
startCommand = "node src/index.js"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
